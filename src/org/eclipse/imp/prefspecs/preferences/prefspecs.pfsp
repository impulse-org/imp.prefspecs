package org.eclipse.imp.prefspecs.preferences;

page PreferenceSpecifications {
    tabs {
        	default out { }
    	    	configuration in { }
    	    	instance in {  }
    	    	project in { }
    }

    fields {
//      font    sourceFont           { defvalue "Courier New" 10 normal; }
        int     tabWidth             { defvalue 4; }
        boolean spacesForTabs        { defvalue false; }
        boolean emitBuildDiagnostics { defvalue true; }
    }
}
